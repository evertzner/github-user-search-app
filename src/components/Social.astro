---
interface Props {
  value?: string;
}

const { value } = Astro.props;
---

<div class={`text-light-stone ${value ? 'opacity-100' : 'opacity-50'}`}>
  <slot />
  <div>{value || 'Not Available'}</div>
</div>
